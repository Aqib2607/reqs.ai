export default {
    plugins: {
        // We are using @tailwindcss/vite, so we do not include tailwindcss here.
        // This file exists to stop PostCSS from searching for config in parent directories.
    },
};
